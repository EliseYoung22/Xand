<%= form_for(cryo_product) do |f| %>
  <% if cryo_product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cryo_product.errors.count, "error") %> prohibited this cryo_product from being saved:</h2>

      <ul>
      <% cryo_product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :subtitle %>
    <%= f.text_field :subtitle %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div class="field">
    <%= f.label :image %>
    <%= f.text_field :image %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
